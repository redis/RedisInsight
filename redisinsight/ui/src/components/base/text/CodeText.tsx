import React from 'react'
import { Typography, TypographyCodeProps } from '@redis-ui/components'

const CodeText = (props: TypographyCodeProps) => <Typography.Code {...props} />

export { CodeText }
export type { TypographyCodeProps as CodeTextProps }
