import styled from 'styled-components'
import { Row } from 'uiSrc/components/base/layout/flex'

export const InlineRow = styled(Row).attrs({ as: 'span' })``

export const EntryIdTooltipWrapper = styled.div`
  max-width: 200px;
`
