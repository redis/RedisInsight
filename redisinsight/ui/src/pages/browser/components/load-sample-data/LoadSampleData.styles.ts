import styled from 'styled-components'

// Styles for panelClassName - using global class wrapper approach
export const popoverClassName = 'load-sample-data-popover'

export const PopoverStyles = styled.div`
  .${popoverClassName} {
    min-width: 380px;
  }
`
