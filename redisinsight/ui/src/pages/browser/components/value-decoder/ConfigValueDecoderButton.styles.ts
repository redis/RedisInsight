import styled from 'styled-components'

import { EmptyButton } from 'uiSrc/components/base/forms/buttons'

export const ConfigButton = styled(EmptyButton)`
  font-size: ${({ theme }) => theme.core.font.fontSize.s12};
`
