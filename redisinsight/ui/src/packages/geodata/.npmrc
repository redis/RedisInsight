# Retain yarn-equivalent peer dependency resolution.
# @elastic/eui@34.6.0 declares legacy peer deps (e.g. @types/react@^16) that
# conflict with React 18. Mirrors the lenient resolution yarn used by default.
legacy-peer-deps=true
