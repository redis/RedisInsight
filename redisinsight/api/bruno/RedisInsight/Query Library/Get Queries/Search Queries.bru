meta {
  name: Search Queries
  type: http
  seq: 2
}

get {
  url: {{API_URL}}/databases/{{DB_INSTANCE_ID}}/query-library
  body: none
  auth: inherit
}

params:query {
  indexName: {{REDIS_SEARCH_INDEX_NAME}}
  search: bikes
}

settings {
  encodeUrl: true
  timeout: 0
}
