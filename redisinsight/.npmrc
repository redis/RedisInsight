# Retain yarn-equivalent peer dependency resolution.
# Some dependencies declare legacy peer deps that conflict with newer installed
# versions. This mirrors the lenient resolution yarn used by default.
legacy-peer-deps=true
